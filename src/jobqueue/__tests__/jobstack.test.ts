describe("Job stack", () => {
  test("should create job stack successfully");
  test("should be able to queue and process single job");
  test("should be able to queue and process jobs within max limit");
  test("should be able to queue and cancel jobs with timeout");
  test("should be able to queue and cancel jobs when stack full");
});
